<mvc:View 
	xmlns:core="sap.ui.core" 
	xmlns:mvc="sap.ui.core.mvc" 
	xmlns="sap.m" 
	controllerName="ehb.YMM01_App.controller.Form"
	xmlns:html="http://www.w3.org/1999/xhtml"
	xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
	xmlns:smartTable="sap.ui.comp.smarttable">
	<App>
		<pages>
			<Page title="Put expired batches in restricted use stock">
				<content>
					<!--<Label text="Material" labelFor="mat"/>
					<Select
						id="mat"
						forceSelection="false"
						selectedKey="{/SelectedProduct}"
						items="{
							path: '/batchSet',
							sorter: { path: 'Matnr' }
						}">
						<core:Item key="{Matnr}" text="{Matnr}" />
					</Select>
					
					<Label text="Plant" labelFor="plant"/>
					<Select
						id="plant"
						forceSelection="false"
						selectedKey="{/SelectedProduct}"
						items="{
							path: '/batchSet',
							sorter: { path: 'Werks' }
						}">
						<core:Item key="{Werks}" text="{Werks}" />
					</Select>
					
					<Label text="Batch" labelFor="batch"/>
					<Select
						id="batch"
						forceSelection="false"
						selectedKey="{/SelectedProduct}"
						items="{
							path: '/batchSet',
							sorter: { path: 'Charg' }
						}">
						<core:Item key="{Charg}" text="{Charg}" />
					</Select>
					
					
					<Label text="Local Date" labelFor="DP"/>
					<DatePicker
						id="DP"
						placeholder="Enter Date ..."
						valueFormat="yyyy-MM-dd" displayFormat="long"
						change="handleChange"
						class="sapUiSmallMarginBottom"/>
						
					<CheckBox text="Include charges without stock" selected="true"/>
					<CheckBox text="Update"/>-->
					
					<VBox fitContainer="true">
						<smartFilterBar:SmartFilterBar id="smartFilterBar" entitySet="batchSet" persistencyKey="SmartFilter_Explored" basicSearchFieldName="Matnr" enableBasicSearch="true" >
							<smartFilterBar:controlConfiguration>
								<smartFilterBar:ControlConfiguration key="Matnr" visibleInAdvancedArea="true">
								
								</smartFilterBar:ControlConfiguration>
								<smartFilterBar:ControlConfiguration key="Werks" visibleInAdvancedArea="true">
								
								</smartFilterBar:ControlConfiguration>
								<smartFilterBar:ControlConfiguration key="Charg" visibleInAdvancedArea="true">
									
								</smartFilterBar:ControlConfiguration>
							</smartFilterBar:controlConfiguration>
							<!-- layout data used to make the table growing but the filter bar fixed -->
							<smartFilterBar:layoutData>
								<FlexItemData shrinkFactor="0"/>
							</smartFilterBar:layoutData>
						</smartFilterBar:SmartFilterBar>
						<smartTable:SmartTable initiallyVisibleFields="Matnr,Werks,Charg,Vfdat" id="LineItemsSmartTable" entitySet="batchSet" smartFilterId="smartFilterBar" tableType="Table" useExportToExcel="true"
						  beforeExport="onBeforeExport" useVariantManagement="true" useTablePersonalisation="true" header="Batches" showRowCount="true" persistencyKey="SmartTableAnalytical_Explored" enableAutoBinding="true" class="sapUiResponsiveContentPadding">
							<!-- layout data used to make the table growing but the filter bar fixed -->
							<smartTable:layoutData>
								<FlexItemData growFactor="1" baseSize="0%"/>
							</smartTable:layoutData>
						</smartTable:SmartTable>
					</VBox>
				</content>
			</Page>
		</pages>
	</App>
</mvc:View>